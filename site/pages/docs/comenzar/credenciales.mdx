import { Tab, Tabs } from "nextra-theme-docs";
import Link from "next/link";

# Credenciales

## ¿Qué son las credenciales?

Una credencial de API es una clave secreta que se utiliza para autenticar y autorizar el acceso a los recursos y datos de una API.
Las credenciales de API se utilizan para garantizar que solo los usuarios autorizados puedan acceder y utilizar los servicios y recursos de una API determinada. De esta manera, se asegura la privacidad y la seguridad de los datos y se evita cualquier intento de acceso no autorizado a la API.

## Obtén tus credenciales

Como podrás intuir, las credenciales son una parte fundamental para hacer uso de nuestras API. Para obtener tus credenciales, solo debes seguir los siguientes pasos:

import { Steps } from 'nextra-theme-docs'
 
<Steps>
 
### Paso 1: Regístrate para obtener una cuenta de desarrollador.
 
Registrarse para obtener una cuenta de desarrollador es un proceso rápido y sencillo. Para empezar, solo necesitas iniciar sesión con tu usuario y contraseña o con tu cuenta de GitHub. Para ello, haz clic en el botón que encontrarás a continuación. Si todavía no tienes una cuenta, puedes utilizar este mismo botón para crear una cuenta nueva en pocos minutos.

{<div className="row">

<Link className="btn default" href="hola">Iniciar Sesión / Registrarse</Link>
</div>}

### Paso 2: Guarda tus Keys

Después de recibir tus claves para la API, es importante que las guardes para evitar que se pierdan o sean compartidas con personas o grupos no autorizados.

En la siguiente tabla, se muestra los tipos de claves que puedes obtener.

{

<table>
    <tbody>
        <tr>
            <th>Credencial</th>
            <td>Descripción</td>
        </tr>
        <tr>
            <th>
                <Link className="contrast" href="/docs/api/status">
                    Secret Key
                </Link>
            </th>
            <td>
                Es una clave privada utilizada en la autenticación de las API.
                Se utiliza para establecer una conexión segura entre el cliente
                y el servidor de la API, lo que garantiza que solo aquellos con
                acceso a la clave secreta puedan realizar solicitudes válidas y
                acceder a los recursos protegidos por la API.
            </td>
        </tr>
        <tr>
            <th>
                <Link className="contrast" href="/docs/api/data">
                    Public Key
                </Link>
            </th>
            <td>
                Clave de acceso pública que es visible y no confidencial. Esta
                clave se utiliza en conjunto con un conjunto de dominios
                permitidos para autenticar y autorizar el acceso al mapa
                interactivo.
            </td>
        </tr>
    </tbody>
</table>
}

### Paso 3: Llama a la API

Finalmente, solo necesitas utilizar las claves que te proporcionamos y llamar a la API elegida desde tu aplicación. Si tienes dudas de como levar a cabo este proceso, puedes ir a la sección [API](../api).

</Steps>
